# docker compose up --build
services:
  wayland-builder:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: desktop

    volumes:
      - ./src:/src
      - ./out:/usr/local
      - ./build.sh:/build.sh

    #tty: true
    #stdin_open: true
    # command: /bin/bash /build.sh
    command: sleep 7d
